function NextBestAction({ text }) {
  return (
    <section className="card action-card">
      <span className="eyebrow">Siguiente mejor acción</span>
      <p>{text}</p>
    </section>
  );
}

export default NextBestAction;
