# Postgres (Neon) — usar el connection string POOLED en producción serverless
DATABASE_URL=postgres://user:password@host/key3?sslmode=require

# NextAuth
NEXTAUTH_SECRET=   # generar con: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000

# Fase 2 (fotos):
# BLOB_READ_WRITE_TOKEN=
